import pytest
import ${ppackage} 
from uvclight.tests.testing import configure


@pytest.fixture(scope="session")
def config(request):
    return configure(request, ${ppackage}, 'configure.zcml')


@pytest.fixture(scope="session")
def app(request):
    return example.utils.Application({}, 'app')


@pytest.fixture(scope="session")
def root(request):
    return ${ppackage}.utils.Root('app')

